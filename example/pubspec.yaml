name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  usefull_extensions:
    path: ../

dev_dependencies:
  very_good_analysis: ^3.0.1

flutter:
  uses-material-design: true